# No external dependencies beyond Python standard library for the main application
# The application uses:
# - tkinter (standard library)
# - json (standard library)
# - os (standard library)
# - subprocess (standard library)
# - logging (standard library)
# - datetime (standard library)

# Note: QEMU must be installed separately and available in your system PATH

# Test dependencies (install with pip install -r requirements.txt)
pytest==7.4.0
pytest-cov==4.1.0
pytest-mock==3.11.1
coverage==7.3.0 